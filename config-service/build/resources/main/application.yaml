spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NaniiGheorghe/micro-config-service.git
  security:
    user:
      name: ${USERNAME}
      password: ${PASSWORD}
server:
  port: 8888
